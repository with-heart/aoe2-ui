{
  "name": "import",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "import": "bun run src/index.ts",
    "watch": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "@effect/schema": "^0.62.7",
    "schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
