{
  "name": "aoe2-ui",
  "private": true,
  "author": "with-heart <with.heart@pm.me>",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter=site run dev",
    "build": "pnpm --filter=site run build",
    "type-check": "pnpm -r exec tsc --noEmit",
    "prepare": "husky",
    "import": "pnpm --filter=import run import"
  },
  "dependencies": {
    "@changesets/cli": "^2.27.1",
    "@types/bun": "^1.0.11",
    "eslint": "^8",
    "eslint-config-next": "^14.1.0",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "prettier-plugin-organize-imports": "^3.2.4",
    "syncpack": "^12.3.0"
  },
  "workspaces": [
    "packages/*",
    "site"
  ]
}
